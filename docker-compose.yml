services:
  postgres:
    image: postgres:15.1
    ports:
      - '5466:5432'
    volumes:
      - db_data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: ''
      POSTGRES_HOST_AUTH_METHOD: trust
volumes:
  db_data:
