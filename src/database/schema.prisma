datasource db {
  provider          = "postgresql"
  url               = env("DATABASE_DEV_URL") // TODO: PROD MODE
  shadowDatabaseUrl = env("DATABASE_DEV_URL")
}

generator client {
  provider = "prisma-client-js"
}
