datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model test {
  id    Int    @id @default(autoincrement())
  test  String
  test2 String
}
